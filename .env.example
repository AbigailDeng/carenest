# LLM API Configuration
# 
# IMPORTANT: These are SERVER-SIDE environment variables
# - Do NOT use VITE_ prefix (these are server-side only)
# - Frontend no longer needs API key (uses proxy endpoint)
#
# For local development with proxy server:
LLM_API_KEY=your-api-key-here
LLM_BASE_URL=https://hyperecho-proxy.aelf.dev/v1
LLM_MODEL=vibe-coding-app-gemini
PROXY_PORT=3001

# Optional: If your proxy server is on a different domain in production
# VITE_PROXY_URL=https://your-proxy-server.com/api/llm-proxy
